"use client";

const Spiner = () => {
  return (
    <div
      className={`w-10 h-10 border-4 rounded-full border-slate-600 border-solid border-t-transparent border-b-transparent animate-spin`}
    ></div>
  );
};

export default Spiner;
